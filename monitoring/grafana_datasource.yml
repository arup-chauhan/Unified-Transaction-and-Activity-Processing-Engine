apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:${PROMETHEUS_PORT}
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:${LOKI_PORT}
